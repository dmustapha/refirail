# Stress Test Report — RefiRail
> Generated by hackathon-stress | 2026-06-18
> Confidence Score: 93/100 — Battle-tested

## Summary
| Metric | Value |
|--------|-------|
| Total tests | 33 |
| Passed | 32 |
| Failed (then fixed) | 1 |
| Unresolved | 0 |
| Skipped phases | Phase 5 (no contracts) |
| Confidence | 93/100 |

## Coverage Declaration
| Tier | Features | Coverage Achieved | Skipped Edge Case Classes |
|------|----------|-------------------|---------------------------|
| P0 | F1 position, F2 preview | Exhaustive — validation, security, HTTP method, boundary, concurrency | — |
| P0 | F3 execute | Code-verify only (signing path proven on-chain BiMBPK7…; not re-fired) | live mainnet execution, concurrency, network (all require spending funds) |
| P1 | F4 wallet connect | Sampled (render, autoConnect, DEMO fallback) | unicode, oversized |
| P2 | F5 explorer link | Smoke (URL construction) | concurrency, network, unicode |

## Phase Results

### Phase 2 — Frontend User Journey: PASS
Load → real position renders (PositionCard, live on-chain) → Preview → PreviewPanel + BeforeAfterPanel + Rail render → success path (TxLink) all functional. Honest empty state on no-position address. No console errors at any viewport.

### Phase 2.5 — End-to-End Integration: PASS
| Flow | Duration | Events | Final State | Result |
|------|----------|--------|-------------|--------|
| A1 Refinance preview | ~2.5s warm / ~12–13s cold | ok:true + txB64(4268) + balanceChanges(1) | verified (UI renders fee-$0 + before/after + travelling rail) | PASS |
- gRPC retry layer (lib/retry.ts) proven under 5× parallel load — all returned 200, transient Suilend `reading 'package'` errors caught + retried (healthy, by design). No ASYNC-TIMEOUT / MISSING-EVENTS / NO-FINAL-STATE.
- The real on-chain refinance (3 protocols / 1 PTB) is PROVEN upstream (digest BiMBPK7sLPc1F4DNv4GRseCoLVWPb2oxNdR33Ep8wdsK) — not re-fired (mainnet cost).

### Phase 3 — Visual + A11y: PASS
- 5 viewports (320/768/1024/1440/1920) + 390 mobile: **zero horizontal overflow, zero truncation, zero console errors**.
- Keyboard: first Tab → ConnectButton; `:focus-visible` ring present (computed outline non-none). Reduced-motion honored (design_forge). APR rows carry non-color ↑/↓ cues; `--muted` at WCAG AA.

### Phase 4 — Backend API Stress: PASS (1 fix)
| Test | Result |
|------|--------|
| /api/position valid | 200, real position |
| /api/position missing/empty/invalid-hex/wrong-length | 400 clean, <20ms (no 6s hang — DEV-019) |
| /api/position XSS/SQLi | 400, zero reflection |
| /api/position no-position addr | 200 hasPosition:false honest empty |
| /api/position POST (wrong method) | 405 |
| /api/preview valid | 200 ok:true + txB64 + balanceChanges |
| /api/preview {} / malformed JSON / non-numeric / negative / zero | 400 clean (never 500) |
| /api/preview huge u64-overflow atomics | **was 503 → fixed to 400** (see Fixes) |
| /api/preview 1MB address | 400 fast (25ms) |
| /api/preview XSS address | 400 invalid address |
| /api/preview GET (wrong method) | 405 |
| 5× parallel position + 5× parallel preview | all 200 (retry layer holds) |

### Phase 5 — Contract: SKIPPED (no smart contracts — zero net-new Move; consumes existing protocol packages).

### Phase 6 — State & Data: PASS
- Fetch failure → page.tsx `posError` state ("Couldn't load your position. Check your connection and refresh.") — graceful, no crash.
- Preview fetch failure → catch sets `{ok:false, abortReason}` → PreviewPanel renders "Would revert / try again" — graceful.
- No-position → honest empty card (no fabricated data — INVARIANT 3 held).

### Phase 7 — Network & Timing: PASS
- gRPC retry/backoff proven (1500ms, 3 attempts) under parallel load.
- Cold preview latency ~12–13s (public Suilend gRPC init) — mitigated by Alchemy keyed JSON-RPC (warm ~2.5s) + pre-warm guidance (carry to demo).
- Preview button has a `loading` guard → rapid clicks debounced (no double-submit).

### Phase 8 — Recovery: PASS
- Single-page app; browser refresh re-runs the position `useEffect` (re-fetch on address) → recovers cleanly.
- No multi-step navigation to break; no broken back-button states.

## Fixes Applied
| # | Test ID | File | Description | Verified |
|---|---------|------|-------------|----------|
| 1 | ST-F2-VAL-06 | app/api/preview/route.ts | Out-of-range (u64-overflow) atomics now rejected in the up-front validation layer as a clean **400** ("invalid amount") instead of a downstream **503** — extends DEV-019's client-vs-downstream contract. UI behavior unchanged (real UI never sends such values). | ✓ 400 + valid still 200 + no regression; tsc 0, build 0 |

## Unresolved Issues
None.

## Critique Checkpoint
- Elevations checked: 4 (E-1, E-2, E-3, E-4)
- Verified working: E-1 (DeepBook fee-$0 row in PreviewPanel — rendered), E-3 ($/yr savings in BeforeAfterPanel — rendered). E-2/E-4 are README/sponsor-tracks copy (not a UI surface; unchanged).
- Implemented this session: 0 (all present from build/design_forge)
- Gaps: None.

## Known Operational Caveats (not bugs — carry to demo)
1. Cold /api/preview ~12–13s (public gRPC init). Mitigated: Alchemy keyed JSON-RPC (warm ~2.5s) + pre-warm before recording. Owner: deploy/demo.
2. `healthAfter` renders "—" (the preview path doesn't compute post-health; SF-2 cosmetic, honest). Display-only.
3. Demo position is small (0.30 USDC) → real $/yr savings ≈ $0.01 (honest, not fabricated). Owner: demo (consider a larger position).
