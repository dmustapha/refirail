# DESIGN_BRIEF.md
world_statement: institutional on-chain finance — loan settlement that moves on rails, Sui-native
accent_color: #4DA2FF
background_treatment: deep ink (#060A12) void with a single aqua radial focal glow aligned to the hero — "expensive/atmospheric", no grid, no blobs
typography_domain: Financial / settlement (ronin Section 9) → DM Serif Display (display) / IBM Plex Mono (body + data)
reference_urls: [none provided — domain-derived from the locked world + Sui brand]
rejection_list:
  - Space Grotesk / Inter as display (banned by style.config)
  - generic blue CTA, multi-hue accents, rainbow gradients
  - Dark Mode Cliche (neon glow-on-everything, gradient heading, glass-everywhere)
  - fabricated data — position is read from real on-chain state; honest empty state when none
dials: { DV: 3, MI: 4, VD: 5 }
shadow_philosophy: pure-glow (dark luminous world — elevation via background steps, brand-glow reserved for ≤2 focal elements)
signature_element: "the Rail" — a thin aqua rail under the wordmark (first viewport) that becomes a literal Navi ●─────● Suilend rail in the BeforeAfterPanel, the loan visibly moving from the costlier lender to the cheaper one
tier: B (restraint pass — token system + craft floor applied to the existing proven codebase; no variations/selection)

# Notes
- Mono body is intentional, not a default: style.config "data in monospace where the product is forensic/technical". The screen is APRs, health factors, signed balance deltas, tx digests — a ledger.
- Headline/tagline are NOT model-written: kept verbatim from build ("RefiRail" + "Move your loan to a cheaper rate in one click — one atomic transaction, reverts if it would ever hurt you."). copy-rules §1 honored.
- Pure-glow supersedes the selection preview's "soft-elevation ladder" label: that doctrine is for light worlds; soft drop-shadows are invisible on near-black. Flagged to user.
